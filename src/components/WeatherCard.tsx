type Props={base:string;forecast:string;updated:string};
export function WeatherCard({base,forecast,updated}:Props){return <div className="info-bar"><div><strong>🏨 Base</strong>{base}</div><div><strong>⛅ Weather forecast</strong>{forecast}<small className="weather-updated">Updated {updated}</small></div></div>}