import {AudioButton} from './AudioButton';
export type Item={time:string;title:string;description:string;icon:string;tag?:string;special?:boolean;speech?:string;link?:{label:string;url:string}};
export function Timeline({items}:{items:Item[]}){return <div className="timeline">{items.map((item,i)=><article className={item.special?'timeline-item special':'timeline-item'} key={i}><span className="timeline-dot">{item.icon}</span><div className="time">{item.time}</div><div className="item-card"><h3>{item.title}</h3><p>{item.description}</p>{item.tag&&<span className="tag">{item.tag}</span>}{item.link&&<div className="links"><a href={item.link.url} target="_blank" rel="noreferrer">{item.link.label} ↗</a></div>}{item.speech&&<AudioButton text={item.speech}/>}</div></article>)}</div>}